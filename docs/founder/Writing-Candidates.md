# Founder Writing — Editorial Source Map (Obj 5)

Private. Source: `founders-note-v1.md`. **Candidates only — nothing drafted, nothing
published, no production page touched.** Excerpts are the founder's verbatim words,
cited by question. Confidence = how ready this material is to seed the document
(High / Med / Low). "Combine" notes group excerpts that belong together.

---

## 1 — Founder's Note

| Source | Excerpt | Why it belongs | Conf. |
| --- | --- | --- | --- |
| `fn-origin-moment` | casting Pull Me Under on TikTok Lives; "a match made in heaven… after our second episode… I knew we needed to be called The Luscious Honey Collective… it was more than [the four of us]" | The origin scene — the Note should open here. | High |
| `fn-name` | "we can touch each other with our voices… the instrument that my voice is" | Names the House and its founding gift. | High |
| `fn-why-plain` | "we are more force together collectively than we are individually" | The thesis sentence. | High |
| `fn-inherit` | "Confidence, endurance, patient kindness, and Community" | The inheritance she's passing on. | High |
| `fn-first-seconds` | the "amusement park" of arrival | The feeling on entry. | Med |
| `fn-last-line` | "Protect it at all cost" | The closing line. | High |

**Combine:** open on `fn-origin-moment` → widen with `fn-why-plain` → ground the name
in `fn-name` → close on `fn-inherit` + `fn-last-line`. One continuous first-person arc.

## 2 — Editorial Charter

| Source | Excerpt | Why it belongs | Conf. |
| --- | --- | --- | --- |
| `fn-belief` | "It's not about how good it is. It's about how it makes people feel." | The first principle. | High |
| `fn-protect` | "protect those voices that [have been] silenced or underdeveloped… liberated and continue on the torch" | What the House guards. | High |
| `fn-refuse` | "Chase trends, Flatten voices" | The "we will not" clauses. | High |
| `fn-never-say` | "that an idea was impossible" | A standing prohibition. | High |
| `fn-belong` | "never… require them to be perfect… [or] compromise their own ethics and values" | The membership covenant. | High |
| `fn-conduct` | "Honest without cruelty, Patient with unfinished work, Curious across differences" | Community conduct. | Med |
| `fn-money` | "every artist should be paid… sponsored… uplifted publicly is also a form of compensation" | Compensation clause. | Med |
| `fn-lose-soul` | competition inside the House is the danger | The anti-competition rule. | Med |

**Combine:** `fn-belief` + `fn-bar` (feeling/access) → one standards clause;
`fn-refuse` + `fn-never-say` → one "what we won't do" clause. **Gap:** the two
"owe" questions are unanswered — the *obligations* section needs the follow-ups
before drafting (see Voice-Analysis.md). Conf. Low there.

## 3 — Publishing Manifesto

| Source | Excerpt | Why it belongs | Conf. |
| --- | --- | --- | --- |
| `fn-gap` | "someone who understands not just… creative artist but also operations and good business practices… to grow, stretch, scale" | The problem it answers. | High |
| `fn-for` | "under one roof… not only can we attract the resources, but we are the resource" | The model, in one image. | High |
| `fn-origin-work` | "a two-way flow… the audience is transmuting that art… and feeding us" | The reciprocity argument. | High |
| `fn-belief` | "how it makes people feel" | The publishing standard. | High |
| `fn-protect` | "liberated… continue on the torch" | The mission. | High |
| `fn-oneline` | "a home for storytellers that spans across various means of media" | The one-line definition / possible tagline. | High |

**Combine:** `fn-gap` (the problem) → `fn-for` + `fn-origin-work` (the model) →
`fn-belief` + `fn-protect` (the standard & mission) → `fn-oneline` (the line to end on).

## 4 — House Journal #1

| Source | Excerpt | Why it belongs | Conf. |
| --- | --- | --- | --- |
| `fn-origin-moment` | "the collective wasn't just the four of us… it was more than that" | A real, dated first note about how it began and grew. | Med |
| `fn-first-seconds` | arrival excitement | The feeling to invite readers into. | Med |
| `fn-remembered` | "not anything they knew that they needed but was glad existed" | A warm closing thought. | Med |

**Note:** the Journal is a personal dated entry — seed from these, but it wants a
*fresh* short reflection in her voice (a later, approved drafting step). Conf. Med.

## 5 — Writing Wall Fragment #1

| Source | Excerpt | Why it belongs | Conf. |
| --- | --- | --- | --- |
| `fn-fragment` | "A short glance left her in a trance" | Direct, unfinished, mid-thought — exactly the fragment form. | High |
| `fn-line` (alt) | "Control is the default setting of someone who has no plan" | Stronger as a standalone pull quote than a wall fragment. | Med |

**Recommend:** use `fn-fragment` verbatim for the Wall. Route `fn-line`
("Control is the default setting…") and `fn-last-line` ("Protect it at all cost")
to **pull-quote** use instead. Flag: "You miss 100% of the shots you don't take"
(`fn-words-keep`) is a **borrowed** saying — attribute or set aside; don't present
as original.

---

## Cross-document notes

- **Highest-confidence, reusable lines:** `fn-belief` (feeling), `fn-for` (resource),
  `fn-oneline` (tagline), `fn-protect` (torch), `fn-last-line` (protect it at all cost).
- **Before any drafting:** answer the follow-ups for the two blank "owe" questions and
  the personified voice — they unblock the Charter's obligations and tone sections.
- **Rule:** these remain internal candidates for a later, human-approved drafting
  sprint. The packet is reference material; nothing publishes automatically.
